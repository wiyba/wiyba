# Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more
# ANIMATIONS v1
animations {
    enabled = true
    bezier = myBezier, 0.05, 0.9, 0.1, 1.05
    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
    animation = specialWorkspaceIn, 1, 6, myBezier, slidevert
    animation = specialWorkspaceOut, 1, 6, myBezier, slidevert
}
# ANIMATIONS v2
# animations {
#     enabled = yes

#     bezier = defaultBezier, 0.05, 0.9, 0.1, 1.05

#     animation = windows, 1, 7, defaultBezier
#     animation = windowsOut, 1, 7, default, popin 80%
#     animation = border, 1, 10, default
#     animation = borderangle, 1, 8, default
#     animation = fade, 1, 7, default
#     animation = workspaces, 1, 6, defaultBezier
#     animation = specialWorkspaceIn, 1, 6, defaultBezier, slidevert
#     animation = specialWorkspaceOut, 1, 6, defaultBezier, slidevert
# }
